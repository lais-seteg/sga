{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo \"instalação feita dentro de frontend/\"",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
